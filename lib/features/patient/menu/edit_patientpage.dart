import 'package:flutter/material.dart';

class EditPatientPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Edit Patient Details")),
      body: Center(child: Text("Edit patient information here")),
    );
  }
}
