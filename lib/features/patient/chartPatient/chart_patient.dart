import 'package:flutter/material.dart';

class ChartLabsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Chart Page")),
      body: Center(child: Text("Chart Data will be displayed here")),
    );
  }
}
